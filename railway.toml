# Railway 배포 설정 (Dockerfile 사용 시 자동 감지되므로 최소 설정만)
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# 컨테이너 헬스체크: 루트 페이지 응답
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
